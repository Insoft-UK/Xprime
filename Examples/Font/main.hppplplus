#pragma mode( separator(.,;) integer(h64) )
uses HP;
var CGA := {$I "CGA.h"};

#define WHITE #FFFFFF:32h
#define BLACK 0
export START()
begin
	RECT(BLACK);
	HP.TEXTOUT("CGA Font Example", G0, 0, 0, CGA, WHITE);
	WAIT(-1);
    return 0;
end;

export RESET()
begin
    return 0;
end;
