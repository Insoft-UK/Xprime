#pragma mode( separator(.,;) integer(h64) )
#require <HP>
#require <Fonts>

#define WHITE #FFFFFF:32h
#define BLACK 0
export START()
begin
	RECT(BLACK);
	HP.TEXT("VGA Font Example", 0, 0, Fonts.VGA, WHITE);
	WAIT(-1);
    return 0;
end;

export RESET()
begin
    return 0;
end;