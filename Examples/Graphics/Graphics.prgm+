#pragma mode( separator(.,;) integer(h64) assignment(=) indentation(2) )
#require "GROB"

#include <Common.prgm+>

export START()
begin
	IMAGE_S(G0, 0, 0, img, 20, 240);
	
	WAIT();
    return 0;
end;

export RESET()
begin
	return 0;
end;
