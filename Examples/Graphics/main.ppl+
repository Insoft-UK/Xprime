#pragma mode( separator(.,;) integer(h64) assignment(=) indentation(2) )
uses HP;
local {$I "spectrum.ppl"}

export START()
begin
	RECT();
	HP.IMAGE(G0, 0, 0, spectrum, 20, 240);
	WAIT(-1);
    return 0;
end;

export RESET()
begin
	return 0;
end;
