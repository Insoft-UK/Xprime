#pragma mode( separator(.,;) integer(h64) addon(grob.bmp) indentation(2) )
USES HP;
LOCAL spectrum := {$I "spectrum.bmp"};

EXPORT START()
BEGIN
  RECT();
  HP.IMAGE(G0, 0, 0, spectrum);
  WAIT(-1);
  RETURN 0;
END;

EXPORT RESET()
BEGIN
  RETURN 0;
END;
