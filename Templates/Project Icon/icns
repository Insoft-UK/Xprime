#!/bin/bash
DIR=$(dirname "$0")
clear
cd "$DIR"

iconutil -c icns Project.iconset

read -p "Press Enter to exit!"

# Close the Terminal window
osascript -e 'tell application "Terminal" to close window 1' & exit
